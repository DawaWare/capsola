import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dawaware.capsola',
  appName: 'Capsola',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
